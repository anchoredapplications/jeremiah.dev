import { ReactNode } from "react"

export type Skill = {
    subtitle: string,
    image: ReactNode,
    href: string,
}

export type ImportedSkill = {
    subtitle: string,
    image: ReactNode,
    href: string,
}